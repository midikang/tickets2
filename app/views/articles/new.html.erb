<h1>Write and Publish a New Article</h1>
<% if @article.errors.any? %>
  <div>
    <h2>You'r missing some fields</h2>
    <ul>
      <% @article.errors.full_messages.each do |msg|%>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= render 'form' %>
<%= link_to 'Cancel',articles_path%>