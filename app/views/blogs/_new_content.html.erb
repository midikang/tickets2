<%= form_for(blog_content) do |f| %>
  <% if blog_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_content.errors.count, "error") %> prohibited this blog_content from being saved:</h2>

      <ul>
      <% blog_content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
