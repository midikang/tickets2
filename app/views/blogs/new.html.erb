<h1>New Blog</h1>

<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <%= render :partial=>'new_content', :locals=>{ :blog_content=>BlogContent.new(:blog_id=>@blog.id) }%>
  
  <p>Blog content</p>
  <%= f.field_for :blogContent do |blogContent_form|%>
    <%=blogContent_form.label :content%>
    <%=blogContent_form.text :content%>    
  <%%>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', blogs_path %>
